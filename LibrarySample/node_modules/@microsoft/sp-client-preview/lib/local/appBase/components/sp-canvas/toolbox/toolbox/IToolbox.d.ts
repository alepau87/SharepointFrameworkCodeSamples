import * as React from 'react';
import { ISerializedCanvasControl } from '../../canvas/canvasControl/ISerializedCanvasControl';
import { IToolboxItem } from '../toolboxItem/IToolboxItem';
import Toolbox from './Toolbox';
export interface IToolboxProps extends React.Props<Toolbox> {
    isLoading: boolean;
    isOpen: boolean;
    onItemClick: (itemProps: ISerializedCanvasControl) => void;
    row: number;
    toolboxItems: IToolboxItem[];
    verticalPosition: number;
    errorMessage?: string;
}
export interface IToolboxState {
    verticalPosition: number;
}
